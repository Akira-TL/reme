# Reme MiMo 动态摘要与 7×24 时间线验证

## 自动化

- `npm test`：在最新 `origin/lbx-frontend@7d8acd86` 整合基线上 192/192 通过。
- `npm run lint`：通过。
- `npm run build`：通过，Vite 生产构建完成。
- `npm run test:route-build`：4/4 通过；受限沙箱首次无法监听 `127.0.0.1`，授权本机回环 preview 后通过。
- Python 完整测试：通过，1 项既有 skip；MiMo 摘要定向测试 5/5 通过。
- Ruff：通过；本次修改的 4 个 Python 模块严格 Mypy 通过。仓库级 Mypy 仍报告 `edge_bundle.py` 与 `fall_runtime.py` 的 4 个既有错误，这两个文件与本次 diff 无关。

## 浏览器

- 视口：832×1021、390×844、360×800。
- 横向溢出：三个视口均为 0。
- 日期切换：8 月 8 日显示 27 条与 `11 / 14 / 2` 来源配比；8 月 9 日显示 38 条与 `22 / 13 / 3`，MiMo 输入数分别为 33 和 44。
- 筛选：设备筛选只显示 13 条设备事件；关怀筛选只显示 3 条关怀线程。
- 材料链：做饭场景的 MiMo 发问、本人菜名与授权回答、多源整理、18 秒匿名厨房骨架短片 Mock、接收对象和“已发给女儿”均可见且可展开。
- 语义与稳定性：无重复 id；无 React 运行时异常。未启动本地服务时仅出现预期的 MiMo/Relay 网络失败，并由 UI 明确展示不可用状态。

## 边界

- 数据仍为 `mock_fixture`，不写数据库、localStorage 或 Relay。
- 设备记录是固定 `mock_device_event`，不会伪装为照片或姿态推断；卧姿不会写成“已经睡着”。
- 没有上传、生成或持久化真实视频；附件和“已发给女儿”只包含 Mock 元数据与 Mock 状态。
- MiMo 摘要仅允许真实本地接口；不可用时不回退固定摘要，也没有改变权威告警、媒体授权或家庭通知职责。
