# 05 — 建立静止与动作转变事件基线

**Type:** task

**What to build:** 基于连续关键点和姿态观察识别持续静止、正常转变、跌倒式转变与不确定转变，输出客观事件事实供 B 推理和 C 展示。该模块不得把静止或躺卧直接解释为危险。

**Blocked by:** 02 — 标注姿态与转变并评估样本覆盖。

**Status:** resolved

- [x] 使用明确的时间窗口计算人体中心高度变化、躯干方向变化、关键点速度、转变前后姿态和关键点质量。
- [x] 分开输出 `normal_transition`、`fall_like_transition` 和 `uncertain_transition`，不得使用单帧规则代替时序判断。
- [x] 持续静止通过 `posture_duration_ms` 和 `motion_level` 表达，不在 A 端输出报警、通知家属或风险等级。
- [x] 事件输出符合共享接口中的 `TransitionEvent`，包含起止时间、置信度、客观证据和质量状态。
- [x] 正常坐下、正常躺下、弯腰/下蹲和跌倒式转变在合成评估中分别记录，不将正常动作误报隐藏在总体准确率中。
- [x] 报告合成事件检出情况、候选数量、`uncertain_transition` 比例和错误时间段；真实指标继续等待人工标注。
- [x] 关键点缺失、镜头切换、动作边界不足或证据冲突时能够拒判或输出不确定事件。
- [x] 为时间窗口、持续时间、事件合并、事件去重和质量降级提供确定性自动化测试。
- [x] 提供固定命令生成事件文件和评估报告，同一输入重复运行保持事件顺序与 schema 稳定。

## Answer

已完成确定性时序基线、共享 `TransitionEvent` 合同、离线候选生成命令和 11 个合成轨迹测试。现有 2370 帧关键点流在没有同步姿态观察时生成 42 个 `uncertain_transition` 候选和 1 个关键点不足区间，没有生成跌倒准确率声明。完整证据、命令、限制与真实标注需求见：

- [`../results/2026-08-01-transition-baseline.md`](../results/2026-08-01-transition-baseline.md)
