# Reme 密集生活时间线实施计划

1. 扩展 Mock fixture，建立生活片段、主动关怀和关联回应的显式、可测试数据模型。
2. 为八日数据覆盖、每日计数、时段分组、Mock 来源和关怀回应关联补充单元测试。
3. 只重构 `/family` 的 `reme` 展示层，加入记忆周提示、摘要、筛选、时段折叠与关怀线程；不改实时 reducer 或授权链路。
4. 用现有 MUI Rounded 图标和既有字体/色彩体系实现获批稿，补齐键盘、焦点和窄屏样式。
5. 运行测试、lint、build 和 route-build；修复回归。
6. 在 390×844 浏览器视口验证主流程，制作目标稿与实现的同屏对比，迭代至 Design QA 通过。
7. 更新验证记录和 `design-qa.md`，提交描述性 Git commit。
