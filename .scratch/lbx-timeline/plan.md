# 家属端独立时间线实施计划

1. 把权威快照到时间线条目的转换提取为纯函数，覆盖初始状态、关键变化、keepalive 去重、换房间清空和容量上限。
2. 新增独立时间线页面：周日期条、当前会话说明、事件列表、展开详情和无历史空状态。
3. 从首页移除内嵌时间线，在底部导航增加第四个「时间线」入口，并更新页面标题与副标题。
4. 调整 390×844 与桌面窄卡片布局，确保固定底栏不遮挡内容、四项标签不拥挤。
5. 运行前端测试、lint、build；启动本地 Viewer，在浏览器检查导航、日期切换、展开/收起、空状态、响应式与 console。
6. 将用户截图的时间线区域与实现截图组合比较，修复 P0/P1/P2，更新 `design-qa.md` 后提交。
